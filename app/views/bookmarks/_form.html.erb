<%= simple_form_for [@list, @bookmark] do |f| %>
   <div class="form-inputs">
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%# <%= f.association :list_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
